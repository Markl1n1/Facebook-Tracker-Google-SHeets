# Telegram Bot - ClientsBot

Telegram-бот для ведения клиентской базы и проверки существующих клиентов по номеру телефона или ссылке. Все данные хранятся в таблице Supabase.

## Основные функции

- ✅ **Проверить клиента**: Проверка существующих клиентов по номеру телефона или ссылке
- ➕ **Добавить клиента**: Добавление новых клиентов в базу данных
- 🔄 **Real-time Updates**: Webhook-based для мгновенного ответа
- ☁️ **Cloud Hosted**: Развертывание на Koyeb для работы 24/7
- 🗄️ **Supabase Integration**: Быстрая и надежная база данных

## Структура меню бота

### Главное меню

После запуска бота командой `/start` пользователь видит главное меню с двумя кнопками:

- ✅ **Проверить** - проверка существующих клиентов
- ➕ **Добавить** - добавление новых клиентов

### Раздел "Проверить"

После нажатия кнопки "Проверить" появляются две опции:

- 🔢 **Проверить по номеру** - проверка клиента по номеру телефона
- 🔗 **Проверить по ссылке** - проверка клиента по ссылке (Facebook, Instagram, Telegram)

**Процесс проверки:**
1. Менеджер выбирает способ проверки (по номеру или по ссылке)
2. Бот запрашивает ввод соответствующего значения
3. Бот проверяет наличие данных в базе Supabase
4. **Если клиент найден:**
   ```
   ✅ Клиент найден.
   Имя: [Имя клиента]
   Добавил: [Имя менеджера]
   Дата: [Дата добавления]
   ```
5. **Если клиент не найден:**
   ```
   ❌ Клиент не найден.
   ```

### Раздел "Добавить"

После нажатия кнопки "Добавить" появляются две опции:

- 🔢 **Добавить по номеру** - добавление клиента по номеру телефона
- 🔗 **Добавить по ссылке** - добавление клиента по ссылке

**Процесс добавления по номеру:**
1. Менеджер вводит номер телефона (автоматически нормализуется: удаляются + и пробелы)
2. Менеджер вводит имя клиента
3. Менеджер вводит свое имя (имя менеджера)
4. Бот автоматически сохраняет данные в Supabase с текущей датой

**Процесс добавления по ссылке:**
1. Менеджер вводит ссылку (может быть Telegram username или ссылка на соцсети: Facebook, Instagram)
2. Менеджер вводит имя клиента
3. Менеджер вводит свое имя (имя менеджера)
4. Бот автоматически сохраняет данные в Supabase с текущей датой

**Важно:**
- При добавлении по номеру поле `phone` заполняется, `facebook_link` остается пустым
- При добавлении по ссылке поле `facebook_link` заполняется, `phone` остается пустым
- Номера телефонов сохраняются без символов `+` и пробелов

## Команды бота

- `/start` - Показать главное меню
- `/help` - Показать справку
- `/cancel` - Отменить текущую операцию

## Требования

Перед развертыванием необходимо:

1. Telegram Bot Token (от [@BotFather](https://t.me/botfather))
2. Проект Supabase с созданной таблицей
3. Аккаунт Koyeb (доступен бесплатный тариф)

## Инструкции по установке

### 1. Создание таблицы в Supabase

#### Шаг 1: Создание проекта Supabase

1. Перейдите на [Supabase](https://supabase.com/)
2. Зарегистрируйтесь или войдите
3. Создайте новый проект
4. Дождитесь готовности проекта (обычно 1-2 минуты)

#### Шаг 2: Создание таблицы

1. В вашем проекте Supabase перейдите в "Table Editor"
2. Нажмите "New Table"
3. Назовите таблицу: `facebook_leads`
4. Добавьте следующие колонки:

| Column Name | Type | Default | Nullable |
|------------|------|---------|----------|
| id | uuid | `gen_random_uuid()` | No (Primary Key) |
| created_at | timestamptz | `now()` | No |
| fullname | text | - | Yes |
| country | text | - | Yes |
| phone | text | - | Yes |
| email | text | - | Yes |
| facebook_id | text | - | Yes |
| facebook_username | text | - | Yes |
| facebook_link | text | - | Yes |
| telegram_user | text | - | Yes |
| manager_name | text | - | Yes |

**SQL скрипт (альтернативный метод):**

Вы можете выполнить SQL скрипт из файла `create_table.sql` в SQL Editor Supabase:

```sql
CREATE TABLE IF NOT EXISTS facebook_leads (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  fullname TEXT,
  country TEXT,
  phone TEXT,
  email TEXT,
  facebook_id TEXT,
  facebook_username TEXT,
  facebook_link TEXT,
  telegram_user TEXT,
  manager_name TEXT
);

-- Индексы для оптимизации поиска
CREATE INDEX IF NOT EXISTS idx_fullname ON facebook_leads(fullname);
CREATE INDEX IF NOT EXISTS idx_email ON facebook_leads(email);
CREATE INDEX IF NOT EXISTS idx_phone ON facebook_leads(phone);
CREATE INDEX IF NOT EXISTS idx_facebook_id ON facebook_leads(facebook_id);
CREATE INDEX IF NOT EXISTS idx_telegram_user ON facebook_leads(telegram_user);
CREATE INDEX IF NOT EXISTS idx_manager_name ON facebook_leads(manager_name);
```

#### Шаг 3: Получение учетных данных Supabase

1. Перейдите в "Settings" → "API"
2. Скопируйте **Project URL** (например, `https://xxxxx.supabase.co`)
3. Скопируйте **service_role** ключ
   - Для этого бота рекомендуется использовать **service_role** ключ, так как RLS отключен

### 2. Развертывание на Koyeb

#### Шаг 1: Создание аккаунта Koyeb

1. Перейдите на [Koyeb](https://www.koyeb.com/)
2. Зарегистрируйтесь для бесплатного аккаунта
3. Подтвердите email

#### Шаг 2: Развертывание из GitHub (Рекомендуется)

1. Загрузите этот код в репозиторий GitHub
2. В панели управления Koyeb нажмите "Create App"
3. Выберите "GitHub" как метод развертывания
4. Подключите ваш аккаунт GitHub
5. Выберите ваш репозиторий
6. Настройте:
   - **Branch**: main (или ваша ветка по умолчанию)
   - **Build command**: (оставьте пустым, используется Procfile)
   - **Run command**: (оставьте пустым, используется Procfile)

#### Шаг 3: Настройка переменных окружения

В настройках приложения Koyeb добавьте следующие переменные окружения:

```
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
WEBHOOK_URL=https://YOUR-APP-NAME.koyeb.app
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_KEY=your_service_role_key_here
TABLE_NAME=facebook_leads
```

**Важно:**
- Замените `YOUR-APP-NAME` на фактическое имя вашего приложения Koyeb (находится в URL приложения)
- Замените `your_service_role_key_here` на ваш service_role ключ Supabase
- Вам нужно будет обновить `WEBHOOK_URL` после первого развертывания, когда вы узнаете фактический URL

#### Шаг 4: Развертывание

1. Нажмите "Deploy"
2. Дождитесь завершения развертывания (обычно 2-3 минуты)
3. Скопируйте URL вашего приложения (например, `https://your-app-name.koyeb.app`)

#### Шаг 5: Обновление Webhook URL

1. Вернитесь в настройки приложения Koyeb
2. Обновите переменную окружения `WEBHOOK_URL` с фактическим URL вашего приложения
3. Сохраните и при необходимости переразверните

### 3. Тестирование бота

1. Откройте Telegram
2. Найдите вашего бота (используйте имя бота от BotFather)
3. Отправьте команду `/start`
4. Попробуйте функции:
   - Нажмите "✅ Проверить" → "🔢 Проверить по номеру" → введите номер
   - Нажмите "➕ Добавить" → "🔢 Добавить по номеру" → следуйте инструкциям

## Структура проекта

```
.
├── main.py              # Основное приложение бота
├── requirements.txt     # Python зависимости
├── Procfile            # Конфигурация развертывания Koyeb
├── create_table.sql     # SQL скрипт для создания таблицы
└── README.md           # Этот файл
```

## Конфигурация

### Структура базы данных

- **Имя таблицы**: `facebook_leads`
- **Колонки**: id, created_at, fullname, country, phone, email, facebook_id, facebook_username, facebook_link, telegram_user, manager_name

### Функциональность бота

#### Проверка клиента

- **По номеру**: Поиск в поле `phone` (нормализованный номер без + и пробелов)
- **По ссылке**: Поиск в поле `facebook_link` (точное совпадение)
- При успешном поиске отображается: имя клиента, имя менеджера, дата добавления
- При отсутствии клиента: сообщение "❌ Клиент не найден."

#### Добавление клиента

- **По номеру**: Сохранение нормализованного номера в `phone`, `facebook_link` остается пустым
- **По ссылке**: Сохранение ссылки в `facebook_link`, `phone` остается пустым
- Автоматическое сохранение текущей даты в `created_at`
- Обязательные поля: номер/ссылка, имя клиента, имя менеджера

### Нормализация номеров телефона

Номера телефонов автоматически нормализуются при сохранении:
- Удаляются символы `+`
- Удаляются пробелы
- Пример: `+7 999 123 45 67` → `79991234567`

## Устранение неполадок

### Бот не отвечает

1. Проверьте логи Koyeb на наличие ошибок
2. Убедитесь, что `TELEGRAM_BOT_TOKEN` правильный
3. Убедитесь, что `WEBHOOK_URL` соответствует URL вашего приложения Koyeb
4. Проверьте, запущено ли приложение (панель управления Koyeb)

### Ошибки подключения к Supabase

1. Убедитесь, что `SUPABASE_URL` правильный (должен заканчиваться на `.supabase.co`)
2. Проверьте, что `SUPABASE_KEY` - это service_role ключ (не anon ключ)
3. Убедитесь, что таблица `facebook_leads` существует в вашем проекте Supabase
4. Проверьте, что все имена колонок совпадают точно (с учетом регистра)
5. Проверьте статус проекта Supabase (должен быть активен)

### Поиск не работает

1. Убедитесь, что в таблице есть данные
2. Проверьте, что имена колонок в Supabase совпадают с кодом
3. Проверьте логи Koyeb на наличие конкретных сообщений об ошибках
4. Протестируйте с простым поисковым запросом

### Проблемы с добавлением

1. Убедитесь, что все имена колонок в Supabase совпадают точно
2. Проверьте, что таблица позволяет NULL значения для необязательных колонок
3. Убедитесь, что service_role ключ имеет права INSERT
4. Проверьте логи Koyeb на наличие ошибок базы данных

### Ошибки развертывания

1. Проверьте логи сборки Koyeb
2. Убедитесь, что все зависимости в `requirements.txt` правильные
3. Убедитесь, что `Procfile` присутствует и правильный
4. Проверьте, что версия Python совместима (3.9+)
5. Убедитесь, что все переменные окружения установлены правильно

## Справочник переменных окружения

| Переменная | Описание | Пример |
|------------|----------|--------|
| `TELEGRAM_BOT_TOKEN` | Токен вашего Telegram бота от BotFather | `123456:ABC-DEF...` |
| `WEBHOOK_URL` | URL вашего приложения Koyeb | `https://app.koyeb.app` |
| `SUPABASE_URL` | URL вашего проекта Supabase | `https://xxxxx.supabase.co` |
| `SUPABASE_KEY` | service_role ключ Supabase | `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...` |
| `TABLE_NAME` | Имя вашей таблицы Supabase | `facebook_leads` |
| `PORT` | Порт сервера (устанавливается Koyeb) | `8000` |

## Заметки по безопасности

- Никогда не коммитьте ваш `.env` файл или ключи Supabase в Git
- Держите токен Telegram бота в секрете
- Используйте переменные окружения для всех чувствительных данных
- service_role ключ имеет полный доступ к базе данных - храните его в секрете!
- Рассмотрите возможность реализации Row Level Security (RLS) политик в Supabase для продакшена
- Регулярно обновляйте ваши API ключи

## Схема базы данных

Бот ожидает следующую структуру таблицы в Supabase:

```sql
CREATE TABLE facebook_leads (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  fullname TEXT,
  country TEXT,
  phone TEXT,
  email TEXT,
  facebook_id TEXT,
  facebook_username TEXT,
  facebook_link TEXT,
  telegram_user TEXT,
  manager_name TEXT
);
```

## Кастомизация

### Изменение логики поиска

Отредактируйте функции `check_by_phone_input()` и `check_by_link_input()` в `main.py` для:
- Изменения алгоритма поиска (точное совпадение, частичное совпадение, regex и т.д.)
- Настройки форматирования результатов

### Изменение формы добавления

Отредактируйте обработчики разговоров в `main.py` для:
- Добавления/удаления полей
- Добавления правил валидации
- Изменения порядка полей
- Настройки подсказок

### Добавление новых команд

1. Создайте новую async функцию для обработчика команды
2. Добавьте ее в функцию `create_telegram_app()`
3. Обновите команду `/help` для включения новой команды

## Поддержка

При возникновении проблем или вопросов:

1. Проверьте раздел устранения неполадок выше
2. Проверьте логи Koyeb на наличие сообщений об ошибках
3. Убедитесь, что структура таблицы Supabase соответствует схеме
4. Протестируйте команды бота пошагово
5. Проверьте панель управления Supabase на целостность данных

## Лицензия

Этот проект предоставляется как есть для вашего использования.
